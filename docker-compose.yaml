services:
  agentarr:
    image: jpyles0524/agentarr:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentarr
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - 5173:5173